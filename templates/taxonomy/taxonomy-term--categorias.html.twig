{# /** * @file 
* Default theme implementation to display a taxonomy term. 
* * Available variables: 
* - url: URL of the current term. 
* - name: (optional) Name of the current term. 
* - content: Items for the content of the term (fields and description).
 * Use 'content' to print them all, or print a subset such as 
 * 'content.description'. Use the following code to exclude the 
 * printing of a given child element: * @code 
 * {{ content|without('description') }} * @endcode 
 * - attributes: HTML attributes for the wrapper. 
 * - page: Flag for the full page state. 
 * - term: The taxonomy term entity, including: 
 * - id: The ID of the taxonomy term. 
 * - bundle: Machine name of the current vocabulary.
  * - view_mode: View mode, e.g. 'full', 'teaser', etc. 
  * * @see template_preprocess_taxonomy_term() 
  * * @ingroup themeable */ #}


 {# Custom link #}
 {# {% set name = ' ', '-', term.name|lower %}|replace({ : })
 url in '/personas-y-proyectos/' ~ name #}

{% set symbol = term.field_category_icon_svg.value %}
<a href="{{ url|replace({'colectivos' : 'personas-y-proyectos','categorias' : 'personas-y-proyectos'}) }}" class="c-link-with-icon">

     <span class="c-link-with-icon__icon">
     {% if symbol %}
{% if symbol == "administracion-sanitaria " %}
        {{ administracion_sanitaria }}
        {% elseif symbol == "profesionales-sanitarios " %}
        {{ profesionales_sanitarios }}
        {% elseif symbol == "asociaciones-de-pacientes " %}
        {{ asociaciones_de_pacientes }}
        {% elseif symbol == "colegios-profesionales " %}
        {{ colegios_profesionales }}
        {% elseif symbol == "industria-farmaceutica " %}
        {{ industria_farmaceutica }}
        {% elseif symbol == "medios-de-comunicacion " %}
        {{ medios_de_comunicacion }}
        {% elseif symbol == "ong-de-ambito-general " %}
        {{ ong_de_ambito_general }}
        {% elseif symbol == "ong-de-ambito-sanitario " %}
        {{ ong_de_ambito_sanitario }}
        {% elseif symbol == "pacientes-y-familiares " %}
        {{ pacientes_y_familiares }}
        {% elseif symbol == "sociedades-cientificas " %}
        {{ sociedades_cientificas }}
        {% elseif symbol == "universidades " %}
        {{ universidades }}
          {% endif %}
     {% else %}
     {{ content.field_imagen_perfil }}
     {% endif %}
      </span>
      {{ name }} 
      </a>